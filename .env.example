# Postgres credentials (used by docker-compose + n8n credentials.json)
POSTGRES_DB=flexworktwin
POSTGRES_USER=fwt
POSTGRES_PASSWORD=fwt

# n8n public URL (used by Editor + Runtime as API base)
VITE_N8N_BASE=http://localhost:5678/webhook
